{
  "name": "hiredesk-scripts",
  "version": "1.0.0",
  "description": "Database management and import scripts for HireDesk",
  "scripts": {
    "import-pricing": "ts-node import-pricing.ts",
    "setup-db": "psql -h localhost -U postgres -d hiredesk -f init-db.sql && psql -h localhost -U postgres -d hiredesk -f pricing-schema.sql"
  },
  "dependencies": {
    "csv-parse": "^5.5.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/csv-parse": "^1.2.2"
  }
} 